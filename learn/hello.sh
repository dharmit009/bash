#!/bin/sh 
##!/bin/bash 

# `sh` is for posix compliance which makes this to run any operating
# system for the most part. 
# `bash` acts like a single instance of a terminal and provides
# more streamlined experience. 

# shebang (#!): 
# The `#` is also known as sharp and `!` is also known as
# bang so they came up a term known as 'shebang'. 

echo -e "Hello, World"

